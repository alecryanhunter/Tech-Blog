{{!-- Where you can add comments --}}
<li data-id={{post.id}}>
    <a href="">
    <section>
        <h2>{{post.title}}</h2>
        <p>{{post.User.name}}</p>
    </section>                
    <p>{{post.body}}</p>
    </a>
</li>